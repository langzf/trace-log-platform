# 错误码规范

| HTTP Status | 场景 | 说明 |
|---|---|---|
| 400 | 无效 JSON | 请求体格式错误 |
| 404 | 资源不存在 | 路由或 taskId 不存在 |
| 413 | 请求体过大 | 单请求超过 1MB |
| 500 | 服务内部异常 | 路由执行异常 |

说明：
- API 统一返回 `ok: false` 与 `error` 字段。
- 返回体中透传 `traceId` 以便定位链路。
